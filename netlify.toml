# TalentOS — static SPA on Netlify
[build]
  command = "npm run build"
  publish = "dist"

# Single-page app: serve index.html for any path (prevents 404s on deep links)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
